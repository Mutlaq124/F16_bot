streamlit>=1.32.0

git+https://github.com/HKUDS/LightRAG.git@main

openai>=1.30.0
groq>=0.9.0
ollama>=0.1.7
sentence-transformers==2.6.1
torch==2.3.0
huggingface-hub==0.23.0

python-dotenv>=1.0.0

faiss-cpu>=1.8.0
neo4j>=5.19.0
tiktoken>=0.7.0

pymupdf>=1.24.0

aiohttp>=3.9.0
nest-asyncio>=1.6.0
requests>=2.31.0

# MinerU PDF extraction (recommended, optional -- falls back to PyMuPDF if not installed)
# pip install "magic-pdf[full]"

# Evaluation (eval/eval_script.py)
tqdm>=4.66.0
tabulate>=0.9.0

# Optional LangSmith tracing (if want to add)
# pip install langsmith
# langsmith>=0.1.0